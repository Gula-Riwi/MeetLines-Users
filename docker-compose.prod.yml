version: '3.8'

services:
<<<<<<< Updated upstream
  meetlines-auth:
    image: ghcr.io/gula-riwi/meetlines-auth:latest
    container_name: meetlines-auth
=======
  meetlines-users:
    image: ghcr.io/gula-riwi/meetlines-users:latest
    container_name: meetlines-users
>>>>>>> Stashed changes
    restart: unless-stopped
    ports:
      - "8081:8081"
    environment:
      # Database Configuration
      DB_HOST: ${DB_HOST}
      DB_PORT: ${DB_PORT:-5432}
      DB_NAME: ${DB_NAME}
      DB_USERNAME: ${DB_USERNAME}
      DB_PASSWORD: ${DB_PASSWORD}
      
      # Keycloak OAuth2 Configuration
      KEYCLOAK_ISSUER_URI: ${KEYCLOAK_ISSUER_URI}
      KEYCLOAK_JWK_SET_URI: ${KEYCLOAK_JWK_SET_URI}
      KEYCLOAK_CLIENT_ID: ${KEYCLOAK_CLIENT_ID}
      KEYCLOAK_CLIENT_SECRET: ${KEYCLOAK_CLIENT_SECRET}
      
      # Spring Configuration
      SPRING_PROFILES_ACTIVE: prod
      
      # JVM Options
      JAVA_OPTS: "-Xmx512m -Xms256m"
    
    networks:
      - meetlines-agent
    
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:8081/actuator/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    
    labels:
<<<<<<< Updated upstream
      - "com.meetlines.service=auth"
=======
      - "com.meetlines.service=users"
>>>>>>> Stashed changes
      - "com.meetlines.environment=production"

networks:
  meetlines-agent:
    external: true
